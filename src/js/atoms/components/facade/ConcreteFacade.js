import Facade from './Facade';
// import CSS from '../frontend/CSS'
// import HTML from '../frontend/HTML'
// import JS from '../frontend/JS'
// import ESLint from '../frontend/ESLint'

export default class WebpackFacade extends Facade {
  /* HACK: will implement */
  /* eslint-disable-next-line class-methods-use-this */
  bundle() {
    // new CSS().execute;
    // new HTML().render;
    // new JS().compile;
    // new ESLint(new JS()).lint;
  }

  /* HACK: will implement */
  /* eslint-disable-next-line class-methods-use-this */
  watch() {
    // new CSS().watch;
    // new HTML().look;
    // new JS().wathing;
    // new ESLint(new JS()).w;
  }
}
